channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cxx-compiler
  - c-compiler
  - cmake
  - make
  - scikit-build
  # Host dependencies
  - python
  - numpy
  - pybind11
  - xtl==0.8.1
  - xtensor==0.27.1
  - xtensor-python==0.29.0
  - evalhyd-cpp==0.1.3
  # Test dependencies
  - numpy
